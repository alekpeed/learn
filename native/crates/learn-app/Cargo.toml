[package]
name = "learn-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "ground-up-learning"
path = "src/main.rs"

[dependencies]
learn-domain = { path = "../learn-domain" }
learn-content = { path = "../learn-content" }
learn-validation-engine = { path = "../learn-validation-engine" }
learn-diagnostic = { path = "../learn-diagnostic" }
learn-learning-engine = { path = "../learn-learning-engine" }
learn-persistence = { path = "../learn-persistence" }
