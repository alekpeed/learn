//! Native GTK4 application shell (relm4). Stub until Phase 7 wires up the
//! window and navigation.

fn main() {
    println!("learn-app: workspace scaffold only, no UI yet (Phase 0)");
}

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {}
}
