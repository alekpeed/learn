//! Adaptive placement test: binary search over a skill order. Operates purely
//! on `order: Vec<String>`, so - like the TypeScript original - it has no
//! dependency on learn-domain at all. Mirrors packages/diagnostic.

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {}
}
