//! Deterministic answer grading: the Rational type, the canonical ExactValue
//! (surd) parser, the 10 answer validators, and the pre-AI wrong-answer
//! diagnoser. Mirrors packages/validation-engine (TypeScript).

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {}
}
