//! The mastery/scheduling engine: EMA scoring, the spaced-repetition ladder,
//! the event-sourcing progress projection, question rotation, prerequisite
//! gating, session assembly, achievements, and related pure projections.
//! Mirrors packages/learning-engine (TypeScript).

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {}
}
