//! The event store and repositories. Depends only on learn-domain, not on
//! learn-learning-engine or learn-content - confirmed against the TypeScript
//! source, and preserved here. rusqlite-backed in Phase 6; empty stub until
//! then. Mirrors packages/persistence (TypeScript).

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {}
}
